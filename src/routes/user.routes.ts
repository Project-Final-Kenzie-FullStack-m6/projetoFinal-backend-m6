import { Router } from "express";

const useRoutes = Router();

useRoutes.post("")
useRoutes.get("")
useRoutes.get("/:id")
useRoutes.patch("/:id")
useRoutes.patch(":/id/address")
useRoutes.delete("/:id")

export default useRoutes;